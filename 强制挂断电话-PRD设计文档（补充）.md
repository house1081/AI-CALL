# AI外呼平台 强制挂断电话 PRD设计文档（开发落地版）

## 版本：V1\.0

## 关联主PRD：极简低成本可盈利AI外呼平台（私有化部署）开发PRD

## 设计目的

为规避客户闲聊、无效通话占用线路资源、增加平台线路成本，同时提升外呼效率，明确强制挂断的触发条件、执行逻辑、落地细节，确保开发写死规则、可直接落地，与原有FreeSWITCH网关、AI模型、业务代码完全适配，不新增冗余开发量。

# 一、强制挂断核心规则（开发写死，不可修改）

核心原则：三层闭环控制（AI模型层\+业务代码层\+网关层），确保触发条件满足后100%执行挂断，不出现漏挂、迟挂情况，同时保证客户体验，避免生硬挂断。

## 1\.1 触发条件（满足任一即执行挂断）

- **条件1：闲聊超时（对话轮次触发）**

    - 定义：客户连续4轮及以上不配合业务沟通、扯无关话题（如闲聊家常、无关咨询、不回应需求提问），视为无效闲聊；

    - 计数规则：每轮客户发言（完成一次语音识别\+AI回复）记为1轮，仅统计“无效闲聊轮次”，有效业务沟通轮次不计数；

    - 触发阈值：连续无效闲聊≥4轮，立即执行挂断。

- **条件2：通话时长超时**

    - 统一设定单通通话最长时长为90秒（开发写死，管理员不可修改）；

    - 计时规则：从通话接通（call\_status=1）开始计时，至通话时长达到90秒时，立即执行挂断；

    - 例外情况：无（即使客户正在进行有效业务沟通，达到90秒也需强制挂断，避免单通通话成本过高）。

- **条件3：客户明确拒绝**

    - 识别关键词：客户明确表达“不需要”“不感兴趣”“没时间”“挂断”“别打了”等拒绝类话术（AI模型实时识别，开发写死拒绝关键词库）；

    - 执行逻辑：一旦识别到拒绝关键词，立即执行挂断，不进行任何多余回应，不纠缠。

- **条件4：客户反复不配合**

    - 定义：AI连续2次主动询问客户需求、预算、痛点后，客户仍不回应、故意岔开话题、拒绝配合，视为不配合；

    - 执行逻辑：触发后立即执行挂断，避免无效占用线路。

## 1\.2 挂断执行流程（开发必遵循，闭环逻辑）

1. 触发判定：满足任一强制挂断条件后，优先由业务代码层发起挂断指令，同时同步通知AI模型；

2. AI话术输出：AI模型立即输出固定结束语（不可修改），话术：“感谢您的时间，祝您生活愉快，再见。”，结束语播放完毕后立即挂断；

3. 网关执行：FreeSWITCH网关接收挂断指令（通过8080 API接口），立即终止通话，释放线路资源；

4. 数据记录：通话记录表中标记“强制挂断”，备注挂断原因（如“闲聊超时”“客户拒绝”“时长超时”），同步更新通话时长、扣费金额等字段；

5. 兜底保障：若业务代码层、AI模型层未正常触发挂断，FreeSWITCH网关层将在通话达到90秒时，强制终止通话，确保无漏挂。

# 二、各层落地细节（开发写死，直接对照开发）

## 2\.1 AI模型层（Qwen:4b，写进系统Prompt）

开发需将以下Prompt写死在AI模型调用逻辑中，确保AI严格执行挂断规则，不可修改Prompt内容：

你是专业企业电话销售，口语简短自然，每句话控制20秒内。【强制挂断规则，必须严格执行】1\. 连续闲聊超过4轮、客户不聊业务、一直扯无关话题 → 主动播放固定结束语并触发挂断；2\. 客户明确拒绝、不需要、没时间 → 立即播放固定结束语并触发挂断，不纠缠；3\. 对话时长超过90秒 → 自动播放固定结束语并触发挂断；4\. 连续2次主动询问需求、预算、痛点，客户不配合 → 播放固定结束语并触发挂断。固定结束语：感谢您的时间，祝您生活愉快，再见。业务规则：1\. 简洁自我介绍，快速切入业务；2\. 主动询问：客户需求、预算、痛点、合作意向；3\. 客户问题精准简答，不啰嗦；4\. 支持随时被客户打断；5\. 通话结束（含强制挂断）严格输出一行结构化结果，用 \| 分隔：客户需求\|客户痛点\|预算\|最佳回访时间\|意向等级\(A/B/C/D\)（A=高意向 B=中意向 C=低意向 D=无意向）。

开发补充：AI模型识别到拒绝关键词、闲聊超时等条件后，除输出结束语外，需同步返回“挂断触发”标识，供业务代码层接收，触发后续挂断操作。

## 2\.2 业务代码层（Python/后端，硬编码实现）

- **全局变量定义（写死）**

    - MAX\_CHAT\_ROUND = 4（最大无效闲聊轮次）；

    - MAX\_CALL\_SECOND = 90（最大通话时长，单位：秒）；

    - REFUSE\_KEYWORDS = \[\&\#34;不需要\&\#34;, \&\#34;不感兴趣\&\#34;, \&\#34;没时间\&\#34;, \&\#34;挂断\&\#34;, \&\#34;别打了\&\#34;, \&\#34;不用了\&\#34;\]（拒绝关键词库，开发写死，不可修改）；

    - END\_WORDS = \&\#34;感谢您的时间，祝您生活愉快，再见。\&\#34;（固定结束语，不可修改）。

- **核心逻辑（开发必写）**

    - 轮次计数：每完成一次客户语音识别\+AI回复，判断是否为无效闲聊，若是则闲聊轮次\+1，否则重置为0；

    - 时长计时：通话接通后，启动计时器，实时计算通话时长，每秒校验一次是否达到90秒；

    - 关键词识别：客户语音转文字后，比对拒绝关键词库，若包含任一关键词，立即触发挂断；

    - 挂断指令：触发条件满足后，先调用TTS接口播放结束语，播放完毕后，通过FreeSWITCH 8080 API（http://localhost:8080/api/hangup）发送挂断指令；

    - 异常处理：若TTS播放失败，直接发送挂断指令，避免长时间占用线路。

## 2\.3 网关层（FreeSWITCH，写死配置）

修改FreeSWITCH拨号配置文件（conf/dialplan/default\.xml），添加时长兜底规则，开发写死，管理员不可修改：

```xml
<extension name="ai_outbound_call">
  <condition field="destination_number" expression="^(.*)$">
    <action application="set" data="max_call_seconds=90"/&gt; <!-- 写死最长通话90秒，兜底挂断 -->
    <action application="set" data="hangup_after_bridge=true"/>
    <action application="bridge" data="sofia/gateway/aliyun_sip/$1"/>
    &lt;action application="python" data="ai_call_control.py"/&gt; <!-- 对接业务代码层，接收挂断指令 -->
  </condition>
</extension>

```

补充配置：FreeSWITCH需开放8080 API端口，允许本地业务代码层调用挂断接口，确保指令正常接收。

# 三、数据记录与前端展示

## 3\.1 数据入库要求（开发必做）

- 通话记录表（call\_record）新增字段：hangup\_type（varchar\(50\)），用于标记挂断类型，可选值：“正常结束”“强制挂断\-闲聊超时”“强制挂断\-时长超时”“强制挂断\-客户拒绝”“强制挂断\-客户不配合”；

- 强制挂断后，通话记录表需同步更新：通话状态设为“已挂断”，扣费金额、成本金额、毛利按实际通话时长计算（不足1分钟按1分钟计费）；

- AI模型输出的结构化结果（客户需求、痛点、预算等），需正常入库，即使是强制挂断，也需完整记录。

## 3\.2 前端展示要求

- 管理员端：通话记录列表中，强制挂断的通话需标注“强制挂断”及具体原因，便于统计无效通话占比；

- 商户端：通话记录中，强制挂断的通话标注“强制挂断”，不展示具体原因（避免商户误解），仅展示通话时长、扣费金额；

- 管理员端可统计“强制挂断通话数”“各类型强制挂断占比”，用于优化外呼话术、提升有效通话率。

# 四、开发约束（必遵循）

- 所有强制挂断规则、触发阈值、结束语、关键词库，均开发写死，不开放任何配置权限（管理员也不可修改），避免误操作导致规则失效；

- 挂断执行流程需确保“先播放结束语、后挂断”，禁止未播放结束语直接挂断，保障客户体验；

- 业务代码层与FreeSWITCH网关层需做好联动，确保挂断指令同步，避免出现“代码触发挂断，网关未执行”的情况；

- 强制挂断后，需立即释放线路资源，确保该线路可快速投入下一轮外呼，提升线路利用率。

# 五、与主PRD关联适配

- 与AI对话规则：强制挂断规则与AI人设、话术规则完全适配，不冲突；

- 与计费规则：强制挂断后的扣费的按实际通话时长计算，遵循“不足1分钟按1分钟计费”“接通计费”规则；

- 与风控规则：本规则纳入全局风控体系，作为“防资源浪费、保利润”的核心风控项，与线路轮循、夜间禁呼等规则并行执行；

- 与数据库表：新增的hangup\_type字段，纳入通话记录表（call\_record），与原有字段无冗余、无冲突。

> （注：文档部分内容可能由 AI 生成）
