# 极简低成本可盈利AI外呼平台（私有化部署）开发PRD（可直接开发·细化版）

## 开发 PRD（最终可直接开发版）

## 版本：V1\.0 极简盈利版（最低成本、最快上线、赚通话差价）

## 修订说明：

本次细化核心：补充「具体字段定义、前端交互流程、后端设计逻辑」，全程贴合“最低成本、极简开发”原则，不新增冗余功能，仅完善开发所需的具体落地细节，确保前后端开发无需额外沟通，直接对照开发。

# 一、项目整体定位（细化补充）

本系统为**私有化部署、多商户计费型 AI 智能外呼平台**，核心是“低成本搭建、高利润变现”，聚焦“线路差价盈利”，砍掉所有非必要功能，优先保障核心流程（外呼、计费、盈利统计）落地。

核心目标（细化）：

1. 对接阿里云 SIP 正规运营商线路，实现批量低价拿货（成本可控），支持线路轮循、状态监控，保障外呼稳定性；

2. 本地部署开源 AI 离线语音套件（FunASR \+ 本地轻量大模型 \+ 本地 TTS），实现零 AI 接口费用，同时保障对话延迟、打断体验，贴合豆包语音对话质感；

3. 支持多商户入驻、预充值、按分钟实时扣费，商户端操作极简，降低用户使用门槛；

4. 核心盈利：赚取“阿里云线路成本价”与“商户付费价”的分钟差价，叠加小额增值服务费，实现稳定盈利；

5. 控制服务器成本（单机器可部署）、降低开发量（复用开源组件），实现快速上线（1\-2周）、快速回本（1\-1\.5个月）。

整体架构：**全网最低成本可商用架构**

> 阿里云线路（通话）\+ 本地私有化 AI（免费）\+ 自研计费后台（盈利核心）
> 
> 

## 1\.1 设计逻辑

以“盈利”为核心，“极简开发”为原则：后端优先复用开源组件（FreeSWITCH、FunASR），减少自研工作量；前端只做核心页面，交互极简；数据层只保留必要字段，不做冗余存储，降低服务器负载。

# 二、整体系统架构（开发必须遵守·细化）

## 2\.1 三层极简架构（无多余组件·细化字段\+交互）

1. **网关层：FreeSWITCH（开源免费）**

    - 核心字段：线路ID、SIP账号、SIP密码、SIP地址、线路状态（启用/禁用）、当前并发数、今日呼出次数、累计呼出次数、成本价

    - 核心功能（细化）：
                

        - 对接阿里云 SIP 线路：接收阿里云 SIP 中继配置，建立长连接，保障通话稳定性；

        - 发起外呼/接收通话：接收后端业务层的外呼指令，拨打客户手机号，返回振铃、接通、无人接听、空号、挂断等状态；

        - 语音流转发：通话接通后，将客户上行语音流（8K采样率）转发至本地AI层，将AI层下行语音流转发至阿里云线路，实现双向语音交互；

        - 状态回调：实时将通话状态（接通时间、挂断时间、通话时长）回调至业务计费层，用于扣费、统计。

    - 交互逻辑：
                

        - 管理员在后台配置阿里云 SIP 线路信息，FreeSWITCH 自动校验连接，连接成功则状态置为“启用”，失败则提示“配置错误”；

        - 外呼任务启动后，FreeSWITCH 从线路池中随机选取“启用”状态的线路，发起拨号，拨号失败则自动切换下一条线路；

        - 通话过程中，实时采集通话时长，通话结束后1秒内，将通话时长、通话状态回调至业务计费层。

2. **AI 层：本地离线 AI 套件（100% 免费，无接口费用）**

    - 核心字段：AI话术ID、话术内容、识别准确率、响应延迟、打断响应时间、意向分级规则、提取字段配置

    - 核心功能（细化）：
                

        - 本地 FunASR：实时流式识别客户语音（8K采样率），转文字准确率≥95%，支持方言基础识别（普通话优先），实时返回识别文本；

        - 本地轻量大模型（Qwen小体量版）：加载管理员配置的全局话术，结合客户语音转文字，实现自由对话、业务问答、需求挖掘，支持上下文记忆（最近3轮对话）；

        - 本地 TTS：流式语音合成，合成延迟≤300ms，音色为“真人销售音”，支持语速调节（默认中等语速），边生成文字边合成语音，无卡顿；

        - 打断功能：实时监听客户语音，检测到客户发声后，200ms内停止当前AI语音播报，清空语音队列，立即响应客户提问；

        - 结构化提取：通话结束后，自动提取客户需求、痛点、预算等5个核心字段，返回至业务计费层，用于入库、展示。

    - 交互逻辑：
                

        - 管理员在后台编辑AI话术，保存后自动同步至本地大模型，无需重启服务，即时生效；

        - 通话接通后，AI层自动触发开场白（从话术模板中提取），同时实时监听客户语音；

        - 客户说话时，FunASR 实时转文字，大模型边识别边生成应答文本，TTS 边生成文本边合成语音，实现“边说边答”；

        - 客户中途插话，AI层立即停止播报，优先识别客户语音，生成对应应答；

        - 通话结束后，AI层在1秒内完成结构化字段提取，同步至业务计费层，与通话记录绑定。

3. **业务计费层：自研后台（本次开发重点·细化字段\+交互\+逻辑）**

    - 核心字段（关联所有业务）：商户ID、线路ID、客户ID、通话ID、充值订单ID、余额、费率、通话时长、扣费金额、成本金额、毛利、通话状态、意向等级

    - 核心功能（细化）：

        - 总管理员后台：线路管理、商户管理、费率配置、风控配置、AI话术配置、数据统计、利润核算；

        - 商户后台：账号管理、余额充值、客户管理、外呼任务管理、通话记录查询、消费明细查询；

        - 计费核心：实时扣费、余额管理、订单管理、利润自动统计；

        - 数据同步：接收网关层的通话状态、AI层的结构化数据，完成数据入库、关联，支持查询、导出。

    - 交互逻辑：
                

        - 管理员操作：登录 → 配置线路 → 配置费率 → 配置AI话术 → 配置风控 → 新增商户 → 查看数据统计；

        - 商户操作：登录 → 充值余额 → 导入客户 → 创建外呼任务 → 启动任务 → 查看通话记录/意向客户 → 查看消费明细；

        - 计费逻辑：外呼任务启动 → 拨号接通 → 实时累计通话时长 → 按分钟扣费（不足1分钟按1分钟） → 通话结束 → 确认扣费金额 → 同步成本、毛利 → 数据入库。

## 2\.2 核心优势（开发无需对接任何第三方付费接口）

- 无阿里云 AI 接口费用，AI 全程本地离线运行；

- 无腾讯 / 讯飞按分钟扣费，降低运营成本；

- 全部 AI 算力内网私有化，数据（录音、对话、客户信息）不外出，保障数据安全；

- **每一通电话纯赚差价，无隐性成本**，盈利模式清晰。

## 2\.3 开发设计逻辑

1\.  优先复用开源组件：FreeSWITCH（网关）、FunASR（ASR）、Qwen轻量模型（LLM）、本地TTS开源组件，减少自研工作量，降低开发成本；

2\.  数据交互极简：网关层、AI层、业务层仅传递核心字段，不传递冗余数据，降低网络负载，保障延迟；

3\.  容错设计：线路拨号失败自动切换、AI识别失败自动重试（最多2次）、余额不足自动停呼，保障系统稳定性；

4\.  可扩展性：预留简单扩展接口（如后续新增话术模板、新增费率档位），但当前版本不开发，避免冗余。

# 三、功能范围（只保留必须开发功能·细化字段\+交互\+逻辑）

## 3\.1 管理员端（盈利核心功能·细化）

### 3\.1\.1 线路管理（核心）

- 核心字段（线路表 line 细化）：
        

    - id：主键（自增，int，非空）

    - sip\_account：SIP账号（varchar\(50\)，非空，唯一，阿里云 SIP 中继账号）

    - sip\_password：SIP密码（varchar\(50\)，非空，加密存储）

    - sip\_address：SIP地址（varchar\(100\)，非空，阿里云 SIP 中继地址，格式：xxx\.aliyuncs\.com:5060）

    - cost\_price：成本价（decimal\(10,4\)，非空，单位：元/分钟，如0\.0600）

    - status：状态（tinyint，非空，0=禁用，1=启用，默认1）

    - daily\_call\_limit：单日最大呼出次数（int，非空，默认1000，可配置）

    - current\_concurrent：当前并发数（int，非空，默认0，实时更新）

    - today\_call\_count：今日呼出次数（int，非空，默认0，每日凌晨清零）

    - create\_time：创建时间（datetime，非空，默认当前时间）

- 交互流程：
        

    1. 管理员进入“线路管理”页面，显示所有线路列表（包含字段：线路ID、SIP账号、成本价、状态、今日呼出次数、当前并发数）；

    2. 点击“新增线路”，弹出表单，填写所有必填字段，点击“保存”，系统自动校验SIP账号、地址、密码的有效性（连接阿里云SIP中继），校验通过则新增成功，状态默认为“启用”；

    3. 点击“编辑”，可修改成本价、单日呼出次数、状态，点击“保存”生效；

    4. 点击“禁用”，线路状态改为0，无法被外呼任务调用；点击“启用”，状态改为1，可正常调用；

    5. 支持线路搜索（按SIP账号、状态）、刷新（实时更新当前并发数、今日呼出次数）。

- 设计逻辑：
       

    - SIP账号唯一，避免重复配置；

    - 成本价保留4位小数，确保计费精准；

    - 实时更新当前并发数、今日呼出次数，便于管理员监控线路负载；

    - 新增线路时自动校验连接，避免配置错误导致外呼失败。

### 3\.1\.2 商户管理（核心）

- 核心字段（商户表 tenant 细化）：
        

    - id：主键（自增，int，非空）

    - username：商户账号（varchar\(50\)，非空，唯一，用于登录）

    - password：密码（varchar\(100\)，非空，MD5加密存储）

    - contact\_name：联系人姓名（varchar\(30\)，非空）

    - contact\_phone：联系人手机号（varchar\(11\)，非空，唯一）

    - balance：余额（decimal\(10,2\)，非空，默认0\.00，单位：元）

    - price\_type：费率类型（tinyint，非空，1=零售价，2=企业价，3=代理价，默认1）

    - sell\_price：每分钟售价（decimal\(10,4\)，非空，根据费率类型默认赋值：零售价0\.1500，企业价0\.1200，代理价0\.0900）

    - status：状态（tinyint，非空，0=禁用，1=启用，默认1）

    - daily\_call\_limit：单日最大呼出次数（int，非空，默认500，可配置）

    - create\_time：创建时间（datetime，非空，默认当前时间）

    - last\_login\_time：最后登录时间（datetime，可空）

- 交互流程：
        

    1. 管理员进入“商户管理”页面，显示所有商户列表（包含字段：商户ID、账号、联系人、手机号、余额、费率类型、售价、状态、最后登录时间）；

    2. 点击“新增商户”，弹出表单，填写所有必填字段，选择费率类型，系统自动填充对应售价，点击“保存”，新增成功，默认状态为“启用”；

    3. 点击“编辑”，可修改联系人、手机号、费率类型、售价、单日呼出次数、状态，点击“保存”生效（修改费率类型时，自动填充对应默认售价，可手动修改）；

    4. 点击“充值”，弹出充值表单，输入充值金额，点击“确认”，商户余额增加，生成充值订单，同步至充值订单表；

    5. 点击“禁用”，商户账号无法登录，正在进行的外呼任务自动停止；点击“启用”，恢复正常；

    6. 支持商户搜索（按账号、联系人、手机号、状态）、查看商户消费明细、通话记录。

- 设计逻辑：
        

    - 商户账号、联系人手机号唯一，避免重复注册；

    - 密码加密存储，保障账号安全；

    - 费率类型与售价绑定，默认填充对应价格，可手动调整，灵活适配不同商户；

    - 禁用商户时，自动停止其外呼任务，避免欠费、违规外呼。

### 3\.1\.3 计费费率配置

- 核心字段（费率配置表，新增，与商户表关联）：
        

    - id：主键（自增，int，非空）

    - price\_type：费率类型（tinyint，非空，1=零售价，2=企业价，3=代理价，唯一）

    - default\_sell\_price：默认售价（decimal\(10,4\)，非空，单位：元/分钟）

    - update\_time：更新时间（datetime，非空，默认当前时间）

- 交互流程：
        

    1. 管理员进入“费率配置”页面，显示3种费率类型的默认售价；

    2. 点击“编辑”，可修改对应费率类型的默认售价，点击“保存”生效；

    3. 修改默认售价后，新增商户时会自动填充修改后的默认值，已存在的商户不受影响（需手动编辑商户售价）。

- 设计逻辑：
        

    - 费率类型唯一，避免重复配置；

    - 默认售价可灵活修改，适配市场价格调整；

    - 已存在商户的售价不受默认值修改影响，保障计费稳定。

### 3\.1\.4 全局风控配置（防封号=保利润）

- 核心字段（风控配置表，新增）：
        

    - id：主键（自增，int，非空）

    - call\_interval：拨打间隔（int，非空，单位：秒，默认30，即两次拨号间隔≥30秒）

    - short\_call\_limit：短通话拦截阈值（int，非空，单位：秒，默认15，即通话时长\&lt;15秒视为短通话，计入风控）

    - call\_start\_time：外呼开始时间（varchar\(5\)，非空，默认09:00）

    - call\_end\_time：外呼结束时间（varchar\(5\)，非空，默认19:00）

    - high\_complaint\_area：高投诉地区（varchar\(200\)，可空，格式：省份1,省份2，如“广东,河南”）

    - update\_time：更新时间（datetime，非空，默认当前时间）

- 交互流程：
        

    1. 管理员进入“全局风控配置”页面，显示所有风控参数；

    2. 编辑各项参数，点击“保存”生效，即时应用到所有外呼任务；

    3. 高投诉地区支持手动输入，用逗号分隔，保存后自动生效，禁止向该地区发起外呼。

- 设计逻辑：
        

    - 拨打间隔、短通话阈值、外呼时间固定配置，所有商户统一适用，简化管理；

    - 高投诉地区可灵活添加、删除，降低投诉率，避免线路封号；

    - 风控参数修改后即时生效，无需重启系统，提升管理效率。

### 3\.1\.5 全平台通话记录、成本统计、利润统计

- 核心字段（复用通话记录表、线路表、商户表，新增统计视图）：
        

    - 统计维度：今日、昨日、近7天、近30天、自定义时间范围

    - 统计字段：总通话次数、总通话时长（分钟）、总扣费金额、总线路成本、总毛利、平均通话时长、接通率

- 交互流程：
       

    1. 管理员进入“数据统计”页面，默认显示今日统计数据；

    2. 选择时间范围，点击“查询”，显示对应时间段的统计数据，支持图表展示（折线图：每日通话量、每日毛利）；

    3. 点击“通话记录明细”，跳转至全平台通话记录列表，可按商户、线路、通话状态、时间范围筛选；

    4. 支持统计数据导出（Excel格式）、通话记录导出（Excel格式）。

- 设计逻辑：
        

    - 统计数据实时计算，基于通话记录表、充值订单表的数据，确保精准；

    - 支持多维度筛选，便于管理员掌握平台运营情况、盈利情况；

    - 导出功能简化数据统计、对账流程。

### 3\.1\.6 AI 全局话术配置（统一销售人设）

- 核心字段（AI话术表，新增）：
        

    - id：主键（自增，int，非空）

    - prompt\_content：话术内容（text，非空，即AI人设模板）

    - opening\_remarks：开场白（varchar\(200\)，非空，如“您好，我是XX公司的销售，打扰您一分钟，想了解一下您这边是否有XX业务的需求？”）

    - end\_remarks：结束语（varchar\(100\)，非空，如“感谢您的时间，后续有需求可以联系我们，再见！”）

    - update\_time：更新时间（datetime，非空，默认当前时间）

- 交互流程：
        

    1. 管理员进入“AI话术配置”页面，显示当前话术内容、开场白、结束语；

    2. 编辑话术内容、开场白、结束语，点击“保存”，自动同步至本地AI层，即时生效；

    3. 支持话术模板保存（可保存多个模板，切换使用），当前版本默认1个模板，满足基础需求。

- 设计逻辑：
        

    - 话术内容支持换行、分段，适配AI模型识别格式；

    - 开场白、结束语单独配置，便于快速修改，无需修改完整话术；

    - 保存后即时同步至AI层，无需重启服务，提升效率。

## 3\.2 商户端（客户使用功能·细化）

### 3\.2\.1 账号登录、余额查看、在线充值

- 核心字段（复用商户表、充值订单表）：

    - 登录字段：username（商户账号）、password（密码）、验证码（可选，简化开发可省略）

    - 余额字段：balance（当前余额）、可用通话分钟数（自动计算：余额÷sell\_price）

    - 充值字段：充值金额（decimal\(10,2\)，非空，最小充值金额100元）、支付方式（默认线下转账，简化开发，无需对接第三方支付）、转账凭证（可选，上传图片）

- 交互流程：


    1. 登录：输入账号、密码，点击“登录”，验证通过后跳转至商户首页；密码错误提示“账号或密码错误”，账号禁用提示“账号已被禁用，请联系管理员”；

    2. 余额查看：首页显示当前余额、可用通话分钟数、今日消费金额、本月消费金额；

    3. 在线充值：点击“充值”，输入充值金额，选择支付方式（线下转账），上传转账凭证（可选），点击“提交”，生成充值订单（状态为“待审核”）；

    4. 充值审核：管理员审核通过后，余额自动到账，订单状态改为“已到账”，商户收到余额到账提示；审核失败，订单状态改为“审核失败”，提示失败原因。

- 设计逻辑：
        

    - 简化支付流程，无需对接第三方支付（降低开发成本），采用线下转账\+管理员审核模式，适合小体量商户；

    - 可用通话分钟数自动计算，便于商户直观了解余额使用情况；

    - 充值订单状态清晰，便于商户、管理员跟踪充值进度。

### 3\.2\.2 客户管理（导入、分组、黑名单）

- 核心字段（客户表 customer 细化）：
        

    - id：主键（自增，int，非空）

    - phone：客户手机号（varchar\(11\)，非空，唯一）

    - name：客户姓名（varchar\(30\)，可空）

    - group\_id：分组ID（int，非空，默认1，关联客户分组表）

    - group\_name：分组名称（varchar\(50\)，非空，默认“未分组”）

    - level：意向等级（varchar\(10\)，可空，A=高意向，B=中意向，C=低意向，D=无意向）

    - is\_black：是否黑名单（tinyint，非空，0=否，1=是，默认0）

    - create\_time：创建时间（datetime，非空，默认当前时间）

    - last\_call\_time：最后通话时间（datetime，可空）

    - tenant\_id：商户ID（int，非空，关联商户表，用于数据隔离）

- 客户分组表（新增，关联客户表）：
        

    - id：主键（自增，int，非空）

    - group\_name：分组名称（varchar\(50\)，非空，如“高意向客户”“待回访客户”）

    - tenant\_id：商户ID（int，非空，关联商户表，确保分组数据隔离）

    - create\_time：创建时间（datetime，非空，默认当前时间）

- 交互流程：
        

    1. 客户导入：点击“导入客户”，下载模板（Excel，包含字段：手机号、姓名），填写后上传，系统校验手机号格式，重复手机号自动跳过，导入成功后提示导入数量、失败数量及原因；

    2. 客户分组：点击“新增分组”，输入分组名称，点击“保存”；选中客户，点击“分组”，选择目标分组，完成分组；支持修改分组名称、删除分组（删除分组时，客户自动归为“未分组”）；

    3. 黑名单管理：选中客户，点击“加入黑名单”，客户is\_black改为1，无法被外呼；点击“移出黑名单”，is\_black改为0，可正常外呼；支持批量加入/移出黑名单；

    4. 客户查询：支持按手机号、姓名、分组、意向等级、是否黑名单筛选，支持搜索。

- 设计逻辑：
        

    - 客户手机号唯一，避免重复导入、重复外呼；

    - 分组数据按商户隔离，避免不同商户数据混淆；

    - 黑名单与外呼任务联动，加入黑名单的客户，外呼任务自动跳过，避免骚扰、投诉。

### 3\.2\.3 创建 AI 外呼任务、启动/暂停

- 核心字段（外呼任务表，新增）：
        

    - id：主键（自增，int，非空）

    - task\_name：任务名称（varchar\(100\)，非空，如“2024年5月装修行业拓客”）

    - tenant\_id：商户ID（int，非空，关联商户表）

    - group\_id：客户分组ID（int，非空，选择要外呼的客户分组）

    - call\_count：计划外呼次数（int，非空，默认等于分组内客户数量）

    - completed\_count：已完成外呼次数（int，非空，默认0）

    - success\_count：接通次数（int，非空，默认0）

    - status：任务状态（tinyint，非空，0=未启动，1=运行中，2=已暂停，3=已完成，4=已终止）

    - start\_time：启动时间（datetime，可空）

    - end\_time：结束时间（datetime，可空）

    - create\_time：创建时间（datetime，非空，默认当前时间）

- 交互流程：
        

    1. 创建任务：点击“创建外呼任务”，输入任务名称，选择客户分组，点击“保存”，任务状态为“未启动”；

    2. 启动任务：点击“启动”，任务状态改为“运行中”，系统自动从选中分组中筛选非黑名单客户，调用FreeSWITCH线路发起外呼；启动前校验商户余额，余额不足提示“余额不足，请先充值”；

    3. 暂停任务：点击“暂停”，任务状态改为“已暂停”，停止外呼，再次点击“启动”可继续运行；

    4. 终止任务：点击“终止”，任务状态改为“已终止”，停止外呼，无法再次启动；

    5. 任务查看：任务列表显示任务名称、分组、计划外呼次数、已完成次数、接通次数、状态、启动时间，点击“详情”可查看该任务下所有通话记录。

- 设计逻辑：

    - 任务状态清晰，便于商户管理外呼进度；

    - 启动任务前校验余额，避免欠费外呼；

    - 外呼时自动跳过黑名单客户，降低投诉率；

    - 任务终止后无法启动，避免重复操作。

### 3\.2\.4 通话记录、录音回放、对话文本

- 核心字段（通话记录表 call\_record 细化）：
        

    - id：主键（自增，int，非空）

    - tenant\_id：商户ID（int，非空，关联商户表）

    - line\_id：线路ID（int，非空，关联线路表）

    - customer\_id：客户ID（int，非空，关联客户表）

    - customer\_phone：客户手机号（varchar\(11\)，非空）

    - call\_duration：通话时长（int，非空，单位：秒，如65秒=1分05秒，按1分钟计费）

    - deduct\_amount：扣费金额（decimal\(10,4\)，非空，单位：元，=通话时长向上取整（分钟）×商户sell\_price）

    - cost\_amount：成本金额（decimal\(10,4\)，非空，单位：元，=通话时长向上取整（分钟）×线路cost\_price）

    - profit：毛利（decimal\(10,4\)，非空，单位：元，=deduct\_amount \- cost\_amount）

    - record\_url：录音地址（varchar\(200\)，非空，本地服务器存储路径，如“/record/20240518/xxx\.wav”）

    - dialog\_text：对话全文（text，可空，AI识别的完整对话文本）

    - customer\_need：客户需求（varchar\(200\)，可空，AI提取）

    - customer\_pain：客户痛点（varchar\(200\)，可空，AI提取）

    - budget：预算范围（varchar\(100\)，可空，AI提取）

    - next\_time：最佳回访时间（varchar\(50\)，可空，AI提取，如“2024\-05\-20 10:00”）

    - level：意向等级（varchar\(10\)，可空，AI提取，A/B/C/D）

    - call\_status：通话状态（tinyint，非空，1=接通，2=无人接听，3=空号，4=拒接，5=拨号失败）

    - call\_time：通话时间（datetime，非空，默认当前时间）

    - task\_id：外呼任务ID（int，可空，关联外呼任务表）

- 交互流程：
       

    1. 通话记录查看：进入“通话记录”页面，显示所有通话记录（包含字段：通话时间、客户手机号、客户姓名、通话时长、扣费金额、意向等级、通话状态）；

    2. 筛选查询：支持按时间范围、客户手机号、通话状态、意向等级、外呼任务筛选；

    3. 录音回放：点击“录音回放”，调用浏览器音频播放器，播放通话录音（支持暂停、倍速）；

    4. 对话文本查看：点击“对话详情”，显示完整对话文本、AI提取的客户需求、痛点、预算等信息；

    5. 导出：支持导出通话记录（Excel格式），包含核心字段。

- 设计逻辑：
        

    - 通话录音本地存储，降低存储成本，保障录音可访问；

    - 通话时长向上取整（不足1分钟按1分钟），符合行业计费规则，保障盈利；

    - AI提取的结构化数据与通话记录绑定，便于商户快速筛选意向客户；

    - 支持多维度筛选，便于商户复盘通话效果。

### 3\.2\.5 客户意向结果查看（A/B/C/D）

- 核心字段（复用客户表、通话记录表）：
        

    - 意向等级：A（高意向）、B（中意向）、C（低意向）、D（无意向）

    - 关联字段：客户手机号、姓名、意向等级、最后通话时间、AI提取的需求、痛点、预算

- 交互流程：
        

    1. 进入“意向客户”页面，默认按意向等级排序（A\&gt;B\&gt;C\&gt;D）；

    2. 支持按意向等级筛选，点击某一客户，可查看该客户的所有通话记录、AI提取的结构化信息；

    3. 支持批量导出意向客户名单（Excel格式），便于商户人工跟进。

- 设计逻辑：
        

    - 意向等级由AI自动提取，无需商户手动标注，降低工作量；

    - 按意向等级排序，便于商户优先跟进高意向客户，提升转化效率。

### 3\.2\.6 个人消费明细

- 核心字段（复用充值订单表、通话记录表）：
        

    - 消费类型：充值、通话扣费

    - 明细字段：时间、类型、金额、余额变动、备注（如“充值100元”“通话扣费0\.15元”）

- 交互流程：
        

    1. 进入“消费明细”页面，显示所有消费记录，按时间倒序排列；

    2. 支持按时间范围、消费类型筛选；

    3. 支持导出消费明细（Excel格式），便于商户对账。

- 设计逻辑：


    - 消费明细实时更新，与充值、通话扣费同步；

    - 备注清晰，便于商户了解每笔收支原因；

    - 导出功能简化商户对账流程。

## 3\.3 AI 核心能力（全部本地离线·细化交互\+逻辑）

- 实时流式 ASR 识别：
        

    - 交互逻辑：客户说话时，FunASR 每20ms采集一次语音片段，实时转文字，同步至大模型，转文字延迟≤200ms，识别准确率≥95%；

    - 设计逻辑：采用流式识别，边说话边转文字，避免等待客户说完再识别，降低整体延迟；本地缓存行业高频词汇（如“业务”“预算”“合作”），提升识别准确率。

- LLM 智能自由对话：
        

    - 交互逻辑：大模型接收ASR转文字，结合管理员配置的话术模板，实时生成应答文本，支持上下文记忆（最近3轮对话），避免重复提问；

    - 设计逻辑：轻量化大模型（Qwen小体量版），本地部署，推理延迟≤300ms；应答文本控制在1\-2句话，简洁明了，符合电话沟通场景；禁止生成冗余内容，降低TTS合成时间。

- 主动需求挖掘：
        

    - 交互逻辑：对话过程中，大模型主动询问客户需求、预算、痛点，如“请问您这边有XX业务的需求吗？”“大概的预算范围是多少呢？”；

    - 设计逻辑：需求挖掘话术嵌入AI人设模板，根据对话进度自动触发，不频繁追问，避免引起客户反感；客户明确拒绝后，不再挖掘需求，直接结束通话。

- 客户意向自动分级：


    - 交互逻辑：通话结束后，大模型根据客户对话内容，自动判定意向等级（A/B/C/D），判定规则嵌入模板（如“客户明确表示有需求、有预算，判定为A级；客户表示考虑，判定为B级”）；

    - 设计逻辑：意向分级规则简单明确，避免复杂判定，提升分级效率；分级结果与通话记录、客户信息绑定，便于后续跟进。

- 流式 TTS 播报、真人打断：
        

    - 交互逻辑：大模型生成应答文本片段（每5\-10个字），TTS立即合成语音，边生成边播报；检测到客户发声（VAD静音检测），200ms内停止播报，清空语音队列，优先识别客户语音；

    - 设计逻辑：TTS采用本地开源音色，合成延迟≤300ms，语速中等，贴合真人销售语气；打断功能优先于播报，提升对话自然度，避免AI抢话、复读。

- 通话结束自动结构化归档：
        

    - 交互逻辑：通话结束后，1秒内完成结构化字段提取（客户需求、痛点、预算、回访时间、意向等级），同步至业务计费层，与通话记录绑定，更新客户表中的意向等级、最后通话时间；

    - 设计逻辑：结构化提取字段固定，不新增冗余字段，确保提取效率；提取失败时，对应字段置空，不影响整体通话记录入库。

# 四、核心盈利规则（开发必须写死·细化字段\+逻辑）

## 4\.1 计费规则（行业通用、利润最大化）

1. **接通计费，未接通不计费**：
        

    - 判定标准：通话状态为“接通”（call\_status=1），开始计费；通话状态为“无人接听、空号、拒接、拨号失败”（call\_status=2\-5），不计费；

    - 设计逻辑：后端接收网关层回调的通话状态，若为“接通”，则计算通话时长并扣费；否则，不扣费，仅记录通话记录，不影响商户余额。

2. **不足 1 分钟按 1 分钟计费**：
        

    - 计算逻辑：通话时长（秒）向上取整，换算为分钟，如10秒=1分钟，65秒=2分钟


