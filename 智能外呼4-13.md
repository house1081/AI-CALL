# 极简低成本可盈利AI外呼平台（私有化部署）开发PRD（可直接开发·细化版）

## 开发 PRD（最终可直接开发版）

## 版本：V1\.0 极简盈利版（最低成本、最快上线、赚通话差价）

## 修订说明：

本次细化核心：补充「具体字段定义、前端交互流程、后端设计逻辑」，全程贴合“最低成本、极简开发”原则，不新增冗余功能，仅完善开发所需的具体落地细节，确保前后端开发无需额外沟通，直接对照开发。

# 一、项目整体定位（细化补充）

本系统为**私有化部署、多商户计费型 AI 智能外呼平台**，核心是“低成本搭建、高利润变现”，聚焦“线路差价盈利”，砍掉所有非必要功能，优先保障核心流程（外呼、计费、盈利统计）落地。

核心目标（细化）：

1. 对接阿里云 SIP 正规运营商线路，实现批量低价拿货（成本可控），支持线路轮循、状态监控，保障外呼稳定性；

2. 本地部署开源 AI 离线语音套件（FunASR \+ 本地轻量大模型 \+ 本地 TTS），实现零 AI 接口费用，同时保障对话延迟、打断体验，贴合豆包语音对话质感；

3. 支持多商户入驻、预充值、按分钟实时扣费，商户端操作极简，降低用户使用门槛；

4. 核心盈利：赚取“阿里云线路成本价”与“商户付费价”的分钟差价，叠加小额增值服务费，实现稳定盈利；

5. 控制服务器成本（单机器可部署）、降低开发量（复用开源组件），实现快速上线（1\-2周）、快速回本（1\-1\.5个月）。

# 二、核心盈利规则（PRD落地设计方案·开发必遵循）

核心原则：开发写死核心计费逻辑，仅开放“价格配置”权限给管理员，确保盈利模式稳定，无漏洞；所有字段、计算逻辑与前文商户表、线路表、通话记录表完全关联，无需额外新增冗余数据表，降低开发成本。

## 2\.1 计费规则（开发写死·不可修改，保障利润最大化）

- **规则1：接通计费，未接通不计费**

    - 落地细节（开发必做）：
                

        - 通话状态判定：仅“接通”状态（对应通话记录表call\_status=1）触发扣费，未接通状态（无人接听=2、空号=3、关机=4、拒接=5、拨号失败=6）均不扣费，不产生成本和毛利；

        - 状态同步逻辑：FreeSWITCH网关层回调通话状态后，后端立即判定，若为未接通状态，通话记录表中deduct\_amount（扣费金额）、cost\_amount（成本金额）、profit（毛利）均赋值为0\.0000；

        - 前端展示：商户端通话记录中，未接通通话标注“未扣费”，管理员端同步标注“无成本、无毛利”，避免歧义。

- **规则2：不足1分钟按1分钟计费**

    - 落地细节（开发必做）：
                

        - 通话时长计算：以通话记录表call\_duration（单位：秒）为准，向上取整为分钟（开发用Math\.ceil\(\)函数，如1秒、59秒均计为1分钟，61秒计为2分钟）；

        - 扣费金额计算：deduct\_amount = 向上取整后的分钟数 × 商户表sell\_price（商户售价，保留4位小数），最终展示给商户时四舍五入保留2位小数；

        - 成本金额计算：cost\_amount = 向上取整后的分钟数 × 线路表cost\_price（线路成本价，保留4位小数），不展示给商户，仅用于管理员利润统计；

        - 异常处理：通话时长为0秒（接通后立即挂断），仍按1分钟计费，避免商户恶意挂断规避扣费。

- **规则3：实时扣费，通话中余额不足立即挂断停止任务**

    - 落地细节（开发必做）：
                

        - 实时扣费频率：通话过程中，每60秒（1分钟）触发一次扣费校验，同步扣除对应金额（按当前已通话分钟数计算，不足1分钟暂不扣，满1分钟立即扣）；

        - 余额校验逻辑：每次扣费前，查询商户表balance（当前余额），若余额 \&lt; 1分钟售价（sell\_price），立即触发挂断指令（FreeSWITCH执行挂断操作），同时停止该商户当前所有外呼任务（任务状态改为2=已暂停）；

        - 提示逻辑：挂断后，商户端弹出提示“余额不足，请立即充值”，同时发送短信通知商户联系人；管理员端同步收到提醒“商户XXX余额不足，已暂停外呼任务”；

        - 补扣处理：若通话结束时，已通话时长不足1分钟（按1分钟计费），且当前余额足够，立即补扣剩余费用；若余额不足，仅扣除当前可用余额，剩余未扣金额记为“待补扣”，商户充值后自动补扣，补扣前无法启动新的外呼任务。

- **规则4：商户账单实时生成**

    - 落地细节（开发必做）：
                

        - 账单生成时机：每完成1通接通通话，立即生成对应账单明细（复用通话记录表，无需新增账单表，降低开发量）；

        - 账单核心字段（复用通话记录表已有字段）：商户ID、通话ID、客户手机号、通话时长、扣费金额、通话时间、线路ID（隐藏）、成本金额（隐藏）；

        - 账单展示：商户端“消费明细”页面，按时间倒序展示所有账单，支持按日期、通话状态筛选，可导出Excel（仅包含商户可见字段）；管理员端可查看所有商户账单，包含隐藏的成本金额、毛利字段；

        - 对账逻辑：账单金额与商户余额变动实时同步，每笔扣费对应一条余额减少记录，管理员端支持“账单与余额变动”对账，确保无扣费异常。

- **规则5：每笔通话记录，明确记录成本价、售价、单笔毛利**

    - 落地细节（开发必做）：
                

        - 字段赋值逻辑（通话记录表字段）：
                        

            - cost\_amount（成本金额）：向上取整后的通话分钟数 × 线路表cost\_price（当前外呼所用线路的成本价）；

            - deduct\_amount（售价/扣费金额）：向上取整后的通话分钟数 × 商户表sell\_price（该商户的当前售价）；

            - profit（单笔毛利）：deduct\_amount \- cost\_amount（保留4位小数，毛利可为正，不可为负，若出现负毛利，后端自动标记异常，提醒管理员检查价格配置）；

        - 权限控制：成本价、单笔毛利仅管理员可见，商户端仅展示扣费金额（售价），避免商户知晓平台成本，影响定价策略；

        - 异常监控：后端新增简单校验逻辑，若单笔毛利\&lt;0，立即记录异常日志，同时在管理员端“数据统计”页面标注异常通话，提醒管理员调整商户售价或线路成本价。

## 2\.2 价格体系（开发开放配置权限·管理员可灵活调整）

- **基础价格档位（开发写死档位，管理员可修改对应价格）**

    - 档位定义（与商户表price\_type字段关联，开发写死映射关系）：
                

        - 1=零售价：默认0\.1500元/分钟（decimal\(10,4\)），适用于个人商户、小体量商户；

        - 2=企业价：默认0\.1200元/分钟（decimal\(10,4\)），适用于中型企业商户（月消费≥5000元）；

        - 3=代理价：默认0\.0900元/分钟（decimal\(10,4\)），适用于代理商户（推荐≥3个商户入驻）；

    - 开发落地细节：
                

        - 价格配置页面：管理员端“费率配置”页面，对应3个档位，每个档位可单独修改默认售价，修改范围限制为0\.0800\-0\.2000元/分钟（开发写死限制，避免售价低于成本价）；

        - 价格生效逻辑：
                        

            - 新增商户时，按选择的费率类型，自动填充对应档位的默认售价，管理员可手动修改该商户单独售价（不影响其他商户）；

            - 管理员修改档位默认售价后，仅对后续新增的商户生效，已存在的商户售价不变，需手动编辑商户售价才能调整；

        - 价格校验：修改售价时，后端自动校验“售价 \&gt; 所有线路的最低成本价”（线路表cost\_price最小值），若低于最低成本价，提示“售价不可低于线路最低成本价，当前最低成本价为XXX元/分钟”，禁止保存。

- **价格调整权限（开发写死权限控制）**

    - 仅总管理员拥有价格修改权限，商户端无任何价格修改权限，仅能查看自己的扣费单价；

    - 价格修改记录：每次修改价格（档位默认价、单个商户售价），后端自动记录修改日志（修改人、修改时间、原价格、新价格），管理员端可查看，便于追溯；

    - 禁止批量修改：开发时禁止新增“批量修改商户售价”功能，避免管理员误操作，导致大面积定价异常。

## 2\.3 利润统计规则（开发自动计算·实时同步，无需手动操作）

- **核心计算逻辑（开发写死，自动执行）**

    - 单笔毛利：profit = deduct\_amount（商户扣费金额） \- cost\_amount（线路成本金额）（保留4位小数，四舍五入后展示给管理员）；

    - 日总利润：每日凌晨0点，系统自动汇总当日所有接通通话的profit字段，生成日利润统计（关联统计视图statistics\_view）；

    - 月总利润：每月月底24点，系统自动汇总当月所有接通通话的profit字段，生成月利润统计，同时留存历史数据，支持按月份查询；

    - 利润统计维度：开发支持按“全平台、单个商户、单个线路、时间范围”统计利润，与前文“数据统计”页面联动，无需额外开发统计页面。

- **落地细节（开发必做）**

    - 实时同步：每完成1通接通通话，系统立即更新日利润、总利润统计数据，延迟≤1秒，确保管理员实时掌握盈利情况；

    - 数据持久化：利润统计数据（日、月）与通话记录同步保存，不可删除，即使删除通话记录（仅管理员可删除），对应的利润数据仍保留，确保对账完整；

    - 异常处理：若出现单笔毛利为负，系统自动标记该通话为“利润异常”，在管理员端数据统计页面单独展示，同时不影响整体利润汇总，提醒管理员及时排查；

    - 导出功能：管理员端支持导出利润统计数据（Excel格式），包含“时间、商户ID、总通话量、总扣费、总成本、总毛利”等核心字段，便于对账、复盘。

## 2\.4 开发约束（必遵循，保障盈利逻辑无漏洞）

- 禁止开发“免扣费”“折扣扣费”等功能，仅保留“按分钟计费”一种核心计费方式；

- 禁止开放“成本价修改”权限给非管理员账号，线路成本价仅管理员可修改，且修改后需重新校验，避免误操作；

- 所有计费、利润计算逻辑，开发时需写死在后端，前端仅负责展示，不可通过前端操作修改计费结果；

- 新增“盈利异常监控”接口（仅管理员可见），实时监控负毛利、扣费异常、余额异常等情况，出现异常立即提醒管理员。

# 五、AI 对话固定规则（开发写死）

## 5\.1 全局 AI 人设（后台可编辑）

默认模板（开发写死初始内容，管理员可在“全局AI话术配置”页面编辑修改，修改后即时同步至本地AI层，无需重启服务）：

你是专业企业销售，电话沟通自然简短。

1. 简洁自我介绍、介绍业务（控制在30秒内，避免冗长，如“您好，我是XX公司的销售，打扰您一分钟，想了解下您这边是否有XX业务需求？”）；

2. 主动询问客户需求、预算、痛点、合作意向（引导式提问，不啰嗦，如“请问您目前有相关需求吗？大概的预算范围是多少？”）；

3. 客户问题精准简答，不啰嗦（单条回答控制在20秒内，不扩展无关内容，避免客户不耐烦）；

4. 客户拒绝立刻礼貌结束，不纠缠（客户明确表示“不需要”“挂断”等，立即说结束语，如“好的，不打扰您了，后续有需求可联系我们，再见”，随后主动挂断通话）；

5. 全程自然真人语气，支持客户随时打断（贴合真人销售沟通节奏，无机械感，客户发声后200ms内停止AI播报，立即响应）；

6. 通话结束自动总结客户需求、痛点、意向等级（无论通话时长，结束后1秒内完成结构化提取，同步至通话记录表）。

## 5\.2 AI 自动提取字段（必须入库，开发写死提取逻辑，不可遗漏）

1. 客户需求 customer\_need（varchar\(200\)，可空，提取客户明确表达的业务需求，如“需要装修服务，想了解报价”，无法提取则留空）；

2. 客户痛点 customer\_pain（varchar\(200\)，可空，提取客户反馈的核心痛点，如“装修报价过高，担心质量不达标”，无法提取则留空）；

3. 预算范围 budget（varchar\(100\)，可空，提取客户提及的预算金额或范围，如“10\-15万”“大概8万左右”，无法提取则留空）；

4. 最佳回访时间 next\_time（varchar\(50\)，可空，提取客户同意的回访时间，如“明天上午10点”“下周一联系”，客户未提及则留空）；

5. 意向等级 level（varchar\(10\)，非空，默认D级，按规则分级：A=高意向（明确表示有需求、愿意进一步沟通）、B=中意向（有需求但需考虑）、C=低意向（无明确需求，不拒绝沟通）、D=无意向（明确拒绝、无需求））。

# 六、全局风控规则（防封号 = 保利润，开发写死可配置，仅管理员可修改参数）

- 单条线路每日最大呼出次数（int，非空，默认1000次，管理员可在“全局风控配置”页面修改，范围100\-10000次，防止单条线路高频呼出被封号）；

- 每次拨打间隔时间（int，非空，默认30秒，管理员可修改，范围10\-60秒，严格控制拨号频率，规避运营商风控）；

- 自动线路轮循（开发写死逻辑，外呼任务启动后，从“启用”状态的线路池中随机选取线路，拨号失败自动切换下一条，直至拨号成功或线路池耗尽，提升外呼成功率）；

- 短通话自动风控拦截（通话时长\&lt;15秒视为短通话，开发写死逻辑，触发后拦截该线路后续拨号5分钟，同时记录风控日志，降低投诉率）；

- 禁止夜间外呼（开发写死时间范围9:00\-19:00可呼，超出该时间范围，系统自动禁止所有外呼任务启动，已启动的任务自动暂停，次日9:00后可手动重启，避免夜间骚扰客户）；

- 全局黑名单库（所有商户共享，开发写死逻辑，管理员可在“全局风控配置”页面添加、删除黑名单手机号，添加后，所有商户的外呼任务均自动跳过该手机号，无法拨号，避免骚扰投诉）。

# 七、数据库表设计（最简 5 张表，够用、不冗余，开发严格按此设计，不新增多余字段）

## 7\.1 线路表 line（核心字段，与网关层FreeSWITCH关联）

- id：主键（int，自增，非空，唯一标识每条线路）

- sip\_account：SIP账号（varchar\(50\)，非空，唯一，阿里云SIP中继账号）

- sip\_password：SIP密码（varchar\(50\)，非空，MD5加密存储）

- sip\_address：SIP地址（varchar\(100\)，非空，阿里云SIP中继地址，格式：xxx\.aliyuncs\.com:5060）

- cost\_price：成本价（decimal\(10,4\)，非空，单位：元/分钟，默认0\.0600）

- status：状态（tinyint，非空，0=禁用，1=启用，默认1）

## 7\.2 商户表 tenant（核心字段，与计费逻辑关联）

- id：主键（int，自增，非空，唯一标识每个商户）

- username：账号（varchar\(50\)，非空，唯一，商户登录账号）

- password：密码（varchar\(100\)，非空，MD5加密存储）

- balance：余额（decimal\(10,2\)，非空，默认0\.00，单位：元）

- price\_type：费率类型（tinyint，非空，1=零售，2=企业，3=代理，默认1）

- sell\_price：每分钟售价（decimal\(10,4\)，非空，按费率类型默认赋值）

- status：状态（tinyint，非空，0=禁用，1=启用，默认1）

## 7\.3 通话记录表 call\_record（核心字段，关联所有业务数据）

- id：主键（int，自增，非空）

- tenant\_id：商户ID（int，非空，关联商户表）

- line\_id：线路ID（int，非空，关联线路表）

- customer\_phone：客户手机号（varchar\(11\)，非空）

- call\_duration：通话时长（int，非空，单位：秒）

- deduct\_amount：扣费金额（decimal\(10,4\)，非空，单位：元）

- cost\_amount：成本金额（decimal\(10,4\)，非空，单位：元）

- profit：毛利（decimal\(10,4\)，非空，单位：元）

- record\_url：录音地址（varchar\(200\)，非空，本地服务器存储路径）

- dialog\_text：对话全文（text，可空，AI识别的完整对话）

- customer\_need：客户需求（varchar\(200\)，可空）

- customer\_pain：客户痛点（varchar\(200\)，可空）

- budget：预算范围（varchar\(100\)，可空）

- next\_time：回访时间（varchar\(50\)，可空）

- level：意向等级（varchar\(10\)，非空，A/B/C/D）

- call\_time：通话时间（datetime，非空，默认当前时间）

## 7\.4 充值订单表 recharge\_order（极简设计，满足充值对账需求）

- order\_no：订单号（varchar\(50\)，非空，唯一，格式：年月日\+随机数，如20260518123456）

- tenant\_id：商户ID（int，非空，关联商户表）

- recharge\_amount：充值金额（decimal\(10,2\)，非空，单位：元）

- arrival\_balance：到账余额（decimal\(10,2\)，非空，充值后商户总余额）

- recharge\_time：充值时间（datetime，非空，默认当前时间）

## 7\.5 客户表 customer（极简设计，满足商户客户管理需求）

- phone：手机号（varchar\(11\)，非空，唯一，主键）

- name：姓名（varchar\(30\)，可空）

- group\_id：分组ID（int，非空，默认1，关联客户分组）

- level：意向等级（varchar\(10\)，可空，A/B/C/D）

- is\_black：是否黑名单（tinyint，非空，0=否，1=是，默认0）

- create\_time：创建时间（datetime，非空，默认当前时间）

# 八、前后端开发页面清单（最小必须页面，不开发任何冗余页面）

## 8\.1 管理员页面（核心盈利管理页面，极简布局）

1. 控制台（核心数据展示：总通话量、总收益、总成本、总利润，实时同步，无需复杂图表，仅展示核心数字）；

2. 线路管理（新增、编辑、启用/禁用线路，查看线路状态、今日呼出次数）；

3. 商户管理（新增、编辑、充值、禁用商户，查看商户余额、消费情况）；

4. 费率配置（修改3个档位默认售价，查看价格修改记录）；

5. 全局 AI 话术配置（编辑AI人设模板、开场白、结束语，支持模板保存、切换）；

6. 全局风控配置（修改风控参数、添加/删除全局黑名单）；

7. 全部通话记录（查看所有商户通话明细，支持筛选、导出，包含成本、毛利字段）；

8. 利润统计报表（按日/月/自定义时间范围统计利润，支持导出，与控制台数据联动）。

## 8\.2 商户页面（操作极简，降低使用门槛）

1. 首页（核心数据展示：当前余额、今日通话次数、今日消费金额、可用通话分钟数）；

2. 客户管理（客户导入、分组、添加/移出黑名单，支持筛选、编辑客户信息）；

3. 外呼任务管理（创建、启动、暂停、终止外呼任务，查看任务进度）；

4. 通话记录 \&amp; 录音回放（查看自身通话明细，播放通话录音、查看对话文本）；

5. 我的账单 / 消费记录（查看自身消费明细，支持筛选、导出，仅展示扣费金额）；

6. 在线充值页面（输入充值金额、上传转账凭证，提交充值申请，查看充值记录）。

# 九、核心接口清单（最简，仅开发必须接口，不新增冗余接口）

1. 线路新增 / 编辑 / 启用禁用接口（对接管理员线路管理页面，同步FreeSWITCH线路配置）；

2. 商户新增 / 充值 / 改费率 / 禁用接口（对接管理员商户管理页面，同步商户余额、费率信息）；

3. 创建外呼任务、启动暂停接口（对接商户端外呼任务管理页面，调用FreeSWITCH发起外呼）；

4. 通话结束回调接口（FreeSWITCH、AI层回调，同步通话时长、扣费金额、AI提取结果至通话记录表）；

5. 余额扣费接口（实时触发，对接计费逻辑，扣除商户余额，同步余额变动）；

6. 充值订单生成接口（对接商户充值页面，生成充值订单，同步商户余额到账）；

7. AI 话术保存接口（对接管理员AI话术配置页面，同步话术至本地AI层）；

8. 数据统计接口（对接管理员控制台、利润统计报表，实时返回统计数据）。

# 十、性能 \&amp; 延迟指标（开发写死，必须满足，保障用户体验）

1. AI 响应延迟：500\-800ms（从客户停止说话到AI开始播报，延迟不超过800ms，确保对话流畅）；

2. 支持打断：200ms 内停止播报（检测到客户发声后，200ms内终止当前AI语音，立即响应客户，贴合真人沟通）；

3. 单服务器支持：50 路并发足够创业盈利（单机器16G内存、8核CPU配置下，稳定支持50路同时通话，无卡顿、无中断）；

4. ASR 识别准确率≥95%（本地FunASR识别客户语音，普通话识别准确率不低于95%，支持基础方言识别）；

5. 通话结束 1 秒内完成结构化数据入库（通话结束后，1秒内将AI提取字段、通话信息同步至通话记录表，确保数据实时性）。

# 十一、本项目坚决不开发（降成本，聚焦核心盈利，不做无用功）

1. 无 APP、无小程序（仅开发PC端网页版，降低开发、维护成本）；

2. 无复杂分销、无多级代理（仅支持单层级商户，不开发分销体系，简化逻辑）；

3. 无大数据大屏（仅展示核心统计数据，不开发复杂可视化大屏，降低开发量）；

4. 无付费 AI 接口（全程使用本地离线AI套件，零AI接口费用）；

5. 无集群部署、无负载均衡（单机器部署即可，降低服务器成本，适合个人/初创）；

6. 无复杂权限、无多余角色（仅保留“总管理员”“商户”两个角色，简化权限管理）。

# 十二、最终商业模式（开发完成即可盈利，模式清晰，无隐性成本）

1. 低价拿阿里线路：以0\.06元/分钟的成本价对接阿里云SIP线路，批量拿货，成本可控；

2. 卖给商户差价盈利：以0\.12\~0\.15元/分钟的价格卖给商户，分三个档位定价，适配不同商户需求；

3. 纯赚稳定差价：每通通话纯赚0\.06\~0\.09元/分钟，无隐性成本，通话量越多，利润越高；

4. 叠加增收渠道：后期可新增商户年费、AI话术定制费等增值服务，进一步提升盈利；

5. 零成本模式：零AI接口成本、零平台月租、零额外服务费，纯利润模式，快速回本。

# 十三、上线最低硬件配置（单机器即可全部跑通，降低投入门槛）

单机器即可部署所有组件，无需多台服务器，一次性投入极低，适合个人/初创团队快速启动：

- 内存：16G（最低不低于8G，确保AI层、网关层稳定运行，无卡顿）；

- CPU：8核（最低不低于4核，保障多并发通话、AI识别流畅）；

- 存储：500G 固态（用于存储通话录音、数据库数据，满足日常运营需求）；

- 部署环境：可本地台式机 / 轻量云服务器（无需高配置服务器，降低硬件成本）；

- 投入成本：一次性硬件投入，无后续月租、维护成本，适合低成本创业。


